

describe('Login Dashboard',() => {
    beforeEach(()=>{
        cy.visit('http://localhost:3000');
       
    })

    it('page login can be opened', () => {
        cy.visit('http://localhost:3000');
       
    })
})